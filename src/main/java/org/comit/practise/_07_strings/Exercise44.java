package org.comit.practise._07_strings;

public class Exercise44 {

	public static void main(String[] args) {
		// Write a Java program to check whether the first two characters appear at the end of the following string.
		 
		
		String str = "Educated";
		//String firstTwoChar = str.charAt(0).concat(str.charAt(1);
		boolean appear = str.endsWith("str.charAt(0) + str.charAt(1)");
		if(appear == true)
			System.out.println("The first two characters appear at the end");
		else
			System.out.println("The first two characters  not appear at the end");
			
		

	}

}
